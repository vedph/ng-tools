import { ColorToContrastPipe } from './color-to-contrast.pipe';

describe('ColorToContrastPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorToContrastPipe();
    expect(pipe).toBeTruthy();
  });
});
